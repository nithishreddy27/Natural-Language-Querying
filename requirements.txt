# Pinned to versions verified working together on this project.
# The ML stack is pinned exactly because loose lower-bounds previously pulled in
# an incompatible/corrupted transformers build. Bump deliberately, then re-test.
torch==2.10.0
transformers==5.0.0
sentence-transformers==5.2.2
faiss-cpu==1.13.2
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.7.2
datasets==4.5.0
tqdm==4.67.1
python-dotenv==1.2.1

# API server (frontend talks to this)
fastapi==0.128.0
uvicorn==0.40.0
pydantic==2.12.5
